# Milestone M13: Command Center - Ready to Publish Platform Visibility

## Objective
Enable operators to see every per-platform post item in the "Ready to Publish" section and retain verified content in the AI Rewrite Dashboard for ongoing control and management.

## Success Criteria
- [x] **`verification_status` column exists** in `content` table (M13S1)
- [x] **"Ready to Publish" section shows individual platform rows** with format: `#ID - Topic | Platform | Content Preview | Status`
- [x] **"AI Rewrite Dashboard" shows verified items** with "Verified - In Queue" badge
- [x] **"Unqueue" button successfully moves** `ready_to_post` → `prepared` with empty `verification_status`
- [x] **Verified content remains visible and actionable** in `/rewrites` view
- [x] **Manual verification flows**: Single Mastodon post flows from idea → approved → prepared → verified → queued → unqueue → prepared

## Phases
### Phase 1: Infrastructure & DB (M13S1)
- Add `verification_status` JSONB column to `content` table. ✅
- Migration script at `scripts/db/migrate_M13_verification_status.py`. ✅

### Phase 2: Backend Query Modifications (M13S2)
- Expand `/rewrites` query to include `ready_to_post` items. ✅
- Add `process_verified_content()` method to filter verified platforms. ✅

### Phase 3: Frontend Ready to Publish Per-Platform Rows (M13S3)
- Dashboard displays per-platform rows with badges. ✅
- `dashboard.html` iterates over `prepared_content` and shows verification status. ✅

### Phase 4: AI Rewrite Dashboard Verified State (M13S4)
- `content_row.html` shows per-platform badges in `/rewrites` view. ✅
- Green badge for verified, gray for unverified, yellow for pending. ✅
## Architectural Improvements (2026-06-28)
- **Duplicate Logic Fixed**: Consolidated verification parsing into `src/autonomedia/core/utils/verification.py`
- **shared utility functions**: `parse_verification_status()`, `is_platform_verified()`, `get_platform_verification()`, `get_verified_at_timestamp()`
- Both `posting_routine.py` and `worker.py` now use shared utilities
    - **Resolved**: Inefficient query now pushes filtering and sorting to DB using JSONB operators; index `idx_content_verified_mastodon` added for `verification_status`.

## Status
- **Completed**: 2026-06-28 (verified)

## Completion Notes
- All phases implemented and verified
- `verification_status` JSONB column added with nested structure for platform metadata
- Templates fixed to correctly access nested `verification_status[platform].verified`
- Tests updated to use correct nested data format
- 48 tests passing across M12/M13/M14 test suites